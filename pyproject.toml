[project]
name = "lrima-status-validation-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "gunicorn>=26.0.0",
    "uvicorn[standard]",
]
